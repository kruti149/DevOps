apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
spec:
  containers:
  - name: nginx-container
    image: nginx
    ports:
    - containerPort: 80
  - name: curl-container
    image: curlimages/curl
    command: ["/bin/sh"]
    args: ["-c", "echo Hello from the another container; sleep 300"]
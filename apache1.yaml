---
- hosts: testservers
  tasks:
  - name : install apache
    yum  : name=httpd state=present 
    notify:
     - start httpd
   
  handlers:
   - name : start httpd
     service : name=httpd state=started

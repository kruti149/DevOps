---
- hosts: testservers
  tasks:
  - name : install apache
    yum  : name=httpd state=present 
    become : true
    become_user : jenkinstest
    notify:
     - start httpd
   
  handlers:
   - name : start httpd
     service : name=httpd state=started

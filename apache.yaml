- name: Install required software
  hosts: testservers

  tasks:
    - yum:
        name: "{{ packages }}"
        state: present
      vars: 
        packages:
         - httpd
      become: yes
        
    - service:
        name: httpd
        state: started
        enabled: yes


